import random\nimport json\nimport time\n\ndef start_game():\n    difficulty = input("Select difficulty (easy, medium, hard, timed): ").lower()\n    if difficulty == 'easy':\n        number_to_guess = random.randint(1, 50)\n        max_attempts = 10\n        base_score = 100\n    elif difficulty == 'medium':\n        number_to_guess = random.randint(1, 100)\n        max_attempts = 7\n        base_score = 70\n    elif difficulty == 'hard':\n        number_to_guess = random.randint(1, 200)\n        max_attempts = 5\n        base_score = 50\n    elif difficulty == 'timed':\n        number_to_guess = random.randint(1, 100)\n        max_attempts = 5\n        base_score = 100\n        start_time = time.time()\n    else:\n        print("Invalid difficulty. Try again.")\n        return\n\n    attempts = 0\n    score = base_score\n    print(f"\nYou have {max_attempts} attempts to guess the number between 1 and {number_to_guess}\n")\n\n    while attempts < max_attempts:\n        guess = int(input("Enter your guess: "))\n        attempts += 1\n        if guess < number_to_guess:\n            print("Too low! Try again.")\n        elif guess > number_to_guess:\n            print("Too high! Try again.")\n        else:\n            print(f"Congratulations! You've guessed the number! Your score is: {score - attempts * 10}")\n            break\n\n        if difficulty == 'timed' and (time.time() - start_time) > 30:\n            print("Time's up! You didn't guess the number in time.")\n            break\n\n        if attempts == max_attempts:\n            print(f"Sorry! The number was {number_to_guess}. Better luck next time!")\n\nif __name__ == '__main__':\n    start_game()\n